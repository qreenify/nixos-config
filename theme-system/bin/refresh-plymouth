#!/usr/bin/env bash

sudo cp ~/.local/share/theme-system/default/plymouth/* /usr/share/plymouth/themes/theme-system/
sudo plymouth-set-default-theme omarchy

if command -v limine-mkinitcpio &>/dev/null; then
  sudo limine-mkinitcpio
else
  sudo mkinitcpio -P
fi
