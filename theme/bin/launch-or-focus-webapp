#!/usr/bin/env bash

if (($# == 0)); then
  echo "Usage: theme-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="theme-launch-webapp $@"

exec theme-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"