#!/usr/bin/env bash

set -e

if [[ $# -lt 1 ]]; then
  echo "Usage: theme-hook [name] [args...]"
  exit 1
fi

HOOK=$1
HOOK_PATH="$HOME/.config/theme/hooks/$1"
shift

if [[ -f $HOOK_PATH ]]; then
  bash "$HOOK_PATH" "$@"
fi
